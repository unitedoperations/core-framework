// [SIDE, TIME, NAME] call sut_fnc_addSetupMarker;
// SIDE is the side of the setup marker, TIME is the time in seconds until the setup marker is removed
// NAME is the name of the setup marker.
// Example:
// [west, (60 * 5), "airfield"] call sut_fnc_addSetupMarker;

[west, (60 * 3), "blu_spawn"] call sut_fnc_addSetupMarker;